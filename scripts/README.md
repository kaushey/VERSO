## Autocomplete

For autocompleting commands source the `./autocomplete.sh` script

## Using verso

To use `verso` on all your folders in your desktop you can export the executable in your `PATH` variable.


```bash
echo 'export PATH=REPO_LOCATION/bin:$PATH' >> ~/.bashrc
```
